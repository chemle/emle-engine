.. _ref-train:

emle.train
==========

The *train* module contains functionality for training EMLE models.
The ``EMLETrainer`` class is used by the ``emle-train`` script for
:ref:`ref_training` EMLE models. It can also be used directly in Python.

.. autoclass:: emle.train.EMLETrainer
   :no-show-inheritance:
